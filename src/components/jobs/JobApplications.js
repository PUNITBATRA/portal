import React from "react";
const JobApplications = ({ job }) => {
  return (
    <h5>
      Job applications for the selected role can be seen here. currently i
      haven't implemented the dummy page for applied student applicants due to
      time constraint. Also as of now we don't have student login.
    </h5>
  );
};
export default JobApplications;
